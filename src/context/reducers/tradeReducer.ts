// src/context/reducers/tradeReducer.ts
const initialState = {
    // Define your initial state here
};

const tradeReducer = (state = initialState, action: any) => {
    switch (action.type) {
        // Define your action types and corresponding state updates here
        default:
            return state;
    }
};

export default tradeReducer;
